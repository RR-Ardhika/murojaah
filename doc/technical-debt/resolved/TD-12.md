# TD-12: Handle Different Version Export/Import

**Status:** ✅ Resolved
**Priority:** Low
**Location:** `src/module/activity/repository/indexeddb/export-import.ts`

---

## Resolution

Resolved by PR #76: Migrate JSStore to Dexie.

- Dexie's `dexie-export-import` handles version in export metadata
- Import function parses both old (v0.1, `inbound: false`) and new (v1, `inbound: true`) formats
- `$types` markers preserve `undefined` vs `0` distinction

---

## Original Description

No version handling when exporting/importing data.

## Risk

Future schema changes may break compatibility with exported data.

## Related

- TD-11: Handle when version is different
